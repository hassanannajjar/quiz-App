import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Quiz = () => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default Quiz;

const styles = StyleSheet.create({});
